{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "break",
  "description": "Default permissions for the break window",
  "windows": ["break"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default"
  ]
}
